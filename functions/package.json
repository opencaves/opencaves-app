{
  "name": "functions",
  "version": "1.0.0",
  "description": "Cloud Functions for OpenCaves",
  "scripts": {
    "lint": "eslint .",
    "dev": "firebase --debug emulators:start --import=.emulator-data/ --export-on-exit",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "type": "module",
  "devDependencies": {
    "eslint": "^8.15.0"
  },
  "private": true
}